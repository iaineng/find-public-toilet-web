/**
 *
 * @param {number | string} addressId 地图id，由附近的厕所接口返回
 * @returns {number} 返回排队的人数
 */
const getToiletQueueNumber = async (addressId) => {
  addressId;
  return 0;
};

export default getToiletQueueNumber;
