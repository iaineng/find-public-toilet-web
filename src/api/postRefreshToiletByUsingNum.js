/**
 *
 * @param {number} maxNum 厕所最大容量人数
 * @param {number} usingNum 使用人数
 * @param {string} userToken
 * @returns {boolean} 是否成功修改
 */
const postRefreshToiletByUsingNum = async (maxNum, usingNum, userToken) => {
  return true;
};
export default postRefreshToiletByUsingNum;
