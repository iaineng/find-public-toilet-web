/**
 * 
 * @param {number} queueNum 排队人数
 * @param {string} userToken
 * @returns {boolean}
 */
const postRefreshToiletByQueueNum = async (queueNum, userToken) => {
  return true;
};

export default postRefreshToiletByQueueNum;
