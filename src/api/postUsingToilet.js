/**
 *
 * @param {string | number} addressId地图id，由附近的厕所接口返回
 * @param {string} userToken 用户的token
 * @returns {boolean} 返回是否使用厕所成功
 */
const postUsingToilet = async (addressId, userToken) => {
  return true;
};
export default postUsingToilet;
