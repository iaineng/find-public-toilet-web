/**
 *
 * @param {string} startTime
 * @param {string} endTime
 * @param {number} addressId
 * @param {string} userToken
 * @returns {boolean}
 */
const postBookToilet = async (startTime, endTime, addressId, userToken) => {
  return true;
};
export default postBookToilet;
