/**
 *
 * @param {string} startTime xx:xx
 * @param {string} endTime xx:xx
 * @param {number} addressId 地图id，由附近的厕所接口返回
 * @param {string} userToken 用户的token
 * @returns {boolean} 返回是否预约厕所成功
 */
const postBookToilet = async (startTime, endTime, addressId, userToken) => {
  return true;
};
export default postBookToilet;
